# Лабораторна робота №1

**Дисципліна:** Тестування програмного забезпечення 

**Тема:** Розробка та тестування програми для аналізу взаємного розміщення прямих на площині

**Варіант:** (2, 4, 5)

**Мова програмування:** Python

**Середовище розробки:** PyCharm

---

## 1. Постановка задачі

Розробити програму, яка:

* приймає на вхід три прямі, задані різними способами (відповідно до варіанту);
* приводить кожну пряму до загального вигляду `Ax + By + C = 0`;
* визначає взаємне розміщення трьох прямих;
* знаходить точки перетину (якщо вони існують);
* коректно обробляє помилкові та некоректні вхідні дані;
* проходить тестування за методом класів еквівалентності та граничних значень.

Програма повинна вивести **одне** з наступних повідомлень:

1. «Прямі співпадають»
2. «Прямі не перетинаються»
3. «Єдина точка перетину прямих (x0, y0)»
4. «Дві точки перетину прямих (x1, y1), (x2, y2)»
5. «Три точки перетину прямих (x1, y1), (x2, y2), (x3, y3)»

---

## 2. Вхідні дані (варіант 2,4,5)

### Пряма 1 — задана через дві точки (формула 2)

Вхідні дані:

* `(x1, y1)`, `(x2, y2)` — дві різні точки

Умова коректності:

* точки не співпадають: `(x1, y1) ≠ (x2, y2)`

### Пряма 2 — задана у відрізках (формула 4)

Рівняння:

```
x/a + y/b = 1
```

Вхідні дані:

* `a`, `b`

Умова коректності:

* `a ≠ 0`, `b ≠ 0`

### Пряма 3 — задана з кутовим коефіцієнтом (формула 5)

Рівняння:

```
y = kx + b
```

Вхідні дані:

* `k`, `b`

Умова коректності:

* `b ≠ 0`

### Обмеження на значення

Усі вхідні параметри — **цілі числа** в діапазоні:

```
[-113; 113]
```

---

## 3. Математичні формули

### 3.1. Загальний вигляд прямої

Усі прямі приводяться до виду:

```
Ax + By + C = 0
```

### 3.2. Приведення до загального вигляду

**Через дві точки:**

```
A = y1 − y2
B = x2 − x1
C = x1·y2 − x2·y1
```

**У відрізках:**

```
x/a + y/b = 1
bx + ay − ab = 0
A = b, B = a, C = −ab
```

**З кутовим коефіцієнтом:**

```
y = kx + b
kx − y + b = 0
A = k, B = −1, C = b
```

---

## 4. Взаємне розміщення прямих

### 4.1. Визначник системи

Для двох прямих:

```
A1x + B1y + C1 = 0
A2x + B2y + C2 = 0
```

визначник:

```
D = A1·B2 − A2·B1
```

### 4.2. Умови

* `D = 0` і коефіцієнти пропорційні → прямі співпадають
* `D = 0` і коефіцієнти не пропорційні → прямі паралельні
* `D ≠ 0` → прямі перетинаються

### 4.3. Формули точки перетину (метод Крамера)

```
x = (B1·C2 − B2·C1) / D
y = (C1·A2 − C2·A1) / D
```

### 4.4. Порівняння з нулем

Для дійсних чисел використовується:

```
ε = 10⁻⁸
|z| ≤ ε → z = 0
```

---

## 5. Класи еквівалентності

### 5.1. Припустимі класи

* **P1** — прямі співпадають
* **P2** — прямі не перетинаються
* **P3** — єдина точка перетину
* **P4** — дві точки перетину
* **P5** — три точки перетину

### 5.2. Неприпустимі класи

* **N1** — неціле значення
* **N2** — значення поза допустимим діапазоном
* **N3** — однакові точки для прямої (2 точки)
* **N4** — `a = 0` або `b = 0` для рівняння у відрізках
* **N5** — `b = 0` для рівняння з кутовим коефіцієнтом

---

## 6. Структура проєкту

```
software_testing_lab1/
│
├── main.py                 # точка входу
├── input_validation.py     # перевірка введених даних
├── line_models.py          # приведення прямих до Ax+By+C
├── line_relations.py       # аналіз взаємного розміщення
├── errors.py               # власні класи винятків
├── test_lab1_unittest.py   # автоматичні юніт-тести (unittest)
├── tests_manual.txt        # ручні тест-кейси
└── README.md               # даний файл
```

---

## 7. Тестування

Тестування виконано:

* методом **класів еквівалентності**;
* методом **граничних значень**;
* ручними тестами (див. `tests_manual.txt`);
* автоматичними юніт-тестами (`unittest`).

Усі автоматичні тести пройдені успішно.

---

## 8. Висновок

У ході лабораторної роботи розроблено програму для аналізу взаємного розміщення трьох прямих, яка:

* відповідає поставленим вимогам;
* коректно обробляє всі допустимі та неприпустимі класи вхідних даних;
* має повний набір тестів;
* є тестопридатною та розширюваною.
